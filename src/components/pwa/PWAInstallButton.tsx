import React from 'react';

/**
 * PWAInstallButton
 * Sembunyikan/hapus menu "Install App" dari tampilan aplikasi sesuai instruksi user (Poin 3).
 */
export const PWAInstallButton: React.FC<{ compact?: boolean; fullWidth?: boolean }> = () => {
  return null;
};
